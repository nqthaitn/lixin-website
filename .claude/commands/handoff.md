Prepare a clean handoff so the next session or agent can resume immediately.

Apply the `handoff-writing` skill — it defines what to include, the rules, and the process
(read task/plan, capture `git diff`/`git status`, write a self-contained handoff, update task status + `TODO.md`).

Repo specifics:
- File: `docs/handoff/YYYY-MM-DD-short-kebab-name.md` from `templates/HANDOFF.template.md`.
- Update the existing handoff for this work instead of creating a duplicate.
